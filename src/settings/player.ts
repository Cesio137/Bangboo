/*
import { YoutubeiExtractor } from "discord-player-youtubei";
import { QueryType } from "discord-player";

export const properties = {
    defaultQuery: QueryType.AUTO,
    queryType: [ 
        QueryType.YOUTUBE, QueryType.YOUTUBE_PLAYLIST, QueryType.YOUTUBE_SEARCH, QueryType.YOUTUBE_VIDEO // Spotify 
    ],
    extractors: [ YoutubeiExtractor ]
}
*/